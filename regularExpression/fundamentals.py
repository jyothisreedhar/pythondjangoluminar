#a regular expression is a special sequence of character that helps you match or find other ,
#strings or set of strings ,using a specialized syntax held in a pattern
step1:import re module
step2:from re import*
#finditer keyword return an iterator yielding match object instances over all
#the string is scanned left to right and matches are returned in the order found
